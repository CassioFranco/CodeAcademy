<?php

    //A definição do caontador
    $x = 0;

    //Inicio / Definição da estrutura
    while($x <= 10){

        //corpo do loop
        echo $x . "\n";
        
        //incremento do contador
        $x++;
    }
    echo "continuando codigo \n";

    //A definição do caontador
    $y = 0;

    //Inicio / Definição da estrutura
    while($y <= 10){

        //corpo do loop
        echo $y . "\n";
        
        //incremento do contador
        $y += 2;
    }


    echo "continuando codigo \n";

    //A definição do caontador
    $z = 10;

    //Inicio / Definição da estrutura
    while($z >= 0){

        //corpo do loop
        echo $z . "\n";
        
        //incremento do contador
        $z--;
    }

    echo "continuando codigo \n";

    //A definição do caontador
    $a = 10;

    //Inicio / Definição da estrutura
    while($a >= 0){

        //corpo do loop
        if($a % 2 !=0){
            echo $a . "\n";
        }
        
        //incremento do contador
        $a--;
    }